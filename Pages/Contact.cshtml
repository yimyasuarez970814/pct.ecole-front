@page
@model pct.ecole_front.Pages.ContactModel
@{
}

<!-- Header Start -->
<div class="jumbotron jumbotron-fluid mb-5">
    <div class="container text-center py-5">
        <h1 class="text-white display-3">Contacto</h1>
        <div class="d-inline-flex align-items-center text-white">
            <p class="m-0"><a class="text-white" asp-page="/Index">Inicio</a></p>
            <i class="fa fa-circle px-3"></i>
            <p class="m-0">Contactenos</p>
        </div>
    </div>
</div>
<!-- Header End -->

@await Html.PartialAsync("Partial/_Contact")
